# Notebooks

Runnable Jupyter notebooks live here.

## Layout

- `feedforward/` - notebooks for dense neural networks and training loops.
- `cnn/` - notebooks for convolutional neural networks.
- `rnn/` - notebooks for recurrent neural networks.
- `sklearn/` - notebooks for classical machine learning baselines.

## Setup

From the repository root:

```python
!pip install -r requirements.txt
```
