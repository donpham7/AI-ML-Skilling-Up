# Sandbox

Use this folder for local experiments, scratch notebooks, temporary datasets, and one-off project ideas.

Everything inside `sandbox/` is ignored by git except this README, so students can experiment freely without committing accidental work.
