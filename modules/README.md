# Modules

This folder contains the course module outline.

Each module folder should hold notes, prompts, references, and assignment descriptions. Runnable notebooks live in `notebooks/`.

## Current modules

- `feedforward/` - dense neural networks and standard supervised learning workflows.
- `cnn/` - convolutional neural networks for image and spatial data.
- `rnn/` - recurrent neural networks for sequence data.
